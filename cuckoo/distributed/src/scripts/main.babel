import DraggableUI              from './lib/DraggableUI';
import { StatsApi }     from './lib/StatsApi';
import widgets from './widgets';

$(function() {

  const UI = new DraggableUI($("#grid"), widgets);

});
